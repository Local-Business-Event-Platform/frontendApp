import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MainStoreContent = () => {
  return (
    <View>
      <Text>MainStoreContent</Text>
    </View>
  );
};

export default MainStoreContent;

const styles = StyleSheet.create({});
